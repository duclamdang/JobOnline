// export function formatCurrency(val?: number | null) {
//   if (val === 0) return "0";
//   if (val == null) return "Thoả thuận";
//   // show in millions if >= 1,000,000
//   if (Math.abs(val) >= 1000000) {
//     return `${(val / 1000000).toLocaleString("vi-VN", { maximumFractionDigits: 0 })} triệu`;
//   }
//   return `${val.toLocaleString("vi-VN")} VND`;
// }
